#Pseudocode to find if a given number is divisible by 3, without using the ‘%’ operator explicitly (like N % 3 == 0).


n = INTEGER(USERINPUT)
WHILE n IS GREATER THAN 0
    n = n - 3
END LOOP

IF n IS EQUAL TO 0 THEN
    OUTPUT "True"
ELSE
    OUTPUT "False"
END IF

END
